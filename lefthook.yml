# lefthook.yml

pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: npx @biomejs/biome check --write --unsafe {staged_files} && git add {staged_files}

    type-check:
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit
